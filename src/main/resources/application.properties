# Porta do servidor (defini a porta padrão)
server.port=8080

# Configuração do banco de dados (useSSL=false - desativa SSL e false&serverTimezone=UTC - evita erro timezone)
spring.datasource.url=jdbc:mysql://localhost:3307/rastreabilidade?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=8570342

# JPA (update - atualiza a tabela BD)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Nome do aplicativo
spring.application.name=rastreabilidade
